# 飞连运维控制台管理员与用户手册

版本基线：当前新版 UI 与当前代码实现状态  
适用对象：管理员、普通使用者、内部培训与交接人员  
文档目标：说明新版控制台怎么使用，并统一解释新版名称、页面、对象、字段和典型操作流程

---

## 1. 系统概述

飞连运维控制台是一个本地 Web 控制台，用来统一完成以下工作：

- 配置飞连连接
- 配置双 LLM 角色模型
- 定义单次飞连 API 请求任务
- 配置飞连周期任务
- 编排运营agent 工作流
- 查看执行日志与系统日志

当前系统可以理解为 5 个核心模块：

- `连接设置`
- `飞连API任务`
- `飞连周期任务`
- `运营agent`
- `Logs`

其中：

- 管理员主要负责配置、任务定义、调度和排障
- 普通使用者主要负责查看任务结果、执行已有流程、阅读日志和运行摘要

---

## 2. 新版导航与名称说明

## 2.1 `Dashboard`

含义：系统首页与总览页。  
作用：

- 展示系统健康状态
- 展示模块能力概览
- 提供快速入口

适合谁：

- 管理员
- 普通使用者

## 2.2 `连接设置`

含义：飞连连接、双模型角色与飞书 API 的配置中心。  
作用：

- 管理飞连环境连接
- 管理 `planner` 与 `formatter` 模型配置
- 管理飞书企业自建应用凭证（`app_id` / `app_secret`）
- 测试连接与热更新

适合谁：

- 管理员

> 连接设置包含三个子页签：`基础信息`、`LLM双模型配置`、`飞书API配置`。其中飞书 API 配置的详细操作见 5.2「飞书 API 配置」。

## 2.3 `飞连API任务`

含义：单次飞连 API 请求任务定义工作台。  
作用：

- 管理 API 模板
- 定义任务草稿
- 执行 Preview / Execute
- 保存为可复用任务草稿

适合谁：

- 管理员
- 高级使用者

## 2.4 `飞连周期任务`

含义：调度中心。  
作用：

- 绑定执行目标
- 配置 cron 或 interval
- 查看最近运行状态
- 手动运行或启停调度

适合谁：

- 管理员

## 2.5 `运营agent`

含义：自动化运营分析与告警工作台。  
作用：

- 编排飞连API任务
- 编排飞连周期任务结果
- 调用 `planner` 与 `formatter`
- 组织 `SKILL.MD / SOUL.MD / prompt`
- 生成分析结果与输出模板

适合谁：

- 管理员
- 高级使用者

## 2.6 `Logs`

含义：日志与排障中心。  
作用：

- 查看执行日志
- 查看系统日志
- 快速定位错误和最近运行情况

适合谁：

- 管理员
- 排障人员

## 2.7 `飞书可信设备导入`（小插件）

含义：将飞连终端设备批量同步至飞书设备管理的工作台。  
作用：

- 同步飞连设备清单（`可信设备` 页）
- 配置飞连 → 飞书字段映射
- 按筛选条件导入 / 更新飞书设备记录
- 查看导入结果与日志

适合谁：

- 管理员

> 详细操作见 5.7「飞书可信设备导入操作指南」。飞书 API 凭证的配置见 5.2「飞书 API 配置」。

---

## 2.8 `自动化审批`（小插件）

含义：监听飞书设备申报事件，对白名单分组内的设备自动通过审批，其余留存为待审批任务并推送通知。  
作用：

- 接收飞书 `device_apply_event_v2` 事件回调
- 在飞连中查询设备是否属于管理员配置的白名单分组
- 命中白名单 → 自动调用飞书新增设备接口，登记为公司设备 + 信任设备
- 未命中 → 留存为待审批任务，并通过配置的 Webhook 推送设备信息
- 支持人工通过 / 驳回待审批任务

适合谁：

- 管理员

> 详细操作见 5.8「自动化审批操作指南」。

---

## 3. 页面截图总览

## 3.1 Dashboard

![Dashboard](ui-screenshots/01-dashboard.png)

## 3.2 连接设置

![连接设置](ui-screenshots/02-connection-settings.png)

## 3.3 飞连API任务

![飞连API任务](ui-screenshots/03-feilian-task.png)

## 3.4 飞连周期任务

![飞连周期任务](ui-screenshots/04-scheduled-task.png)

## 3.5 运营agent

![运营agent](ui-screenshots/05-security-agent.png)

## 3.6 Logs

![Logs](ui-screenshots/06-logs.png)

## 3.7 飞书 API 配置

![飞书API配置](ui-screenshots/07-feishu-api-config.png)

## 3.8 飞书可信设备导入

![飞书可信设备导入](ui-screenshots/08-feishu-device-import.png)

---

## 4. 核心对象与术语解释

## 4.1 `Template`

中文理解：飞连 API 模板。  
作用：定义接口方法、路径和参数结构，便于复用。

常见字段：

- `id`
- `name`
- `category`
- `method`
- `path`
- `query_schema`
- `path_params_schema`
- `body_schema`

一句话理解：

`Template` 是“接口模板”，不是任务。

## 4.2 `Task Draft`

中文理解：任务草稿。  
作用：定义单次飞连 API 请求任务内容，但不带时间计划。

主要字段：

- `id`
- `name`
- `mode`
- `source_template_id`
- `input_config`
- `transform_config`
- `llm_config`
- `output_config`

一句话理解：

`Task Draft` 是“飞连API任务的可复用定义”。

## 4.3 `Job Schedule`

中文理解：飞连周期任务。  
作用：定义什么时候执行哪个目标。

新版核心字段：

- `id`
- `target_type`
- `target_id`
- `enabled`
- `schedule_type`
- `cron`
- `interval`
- `start_at`
- `end_at`
- `timezone`

`target_type` 当前主要支持：

- `task_draft`
- `complex_task`

一句话理解：

`Job Schedule` 负责“调度什么、何时执行”，不负责定义任务内容。

## 4.4 `Complex Task`

中文理解：运营agent 的底层工作流对象。  
作用：组织多步骤流程。

主要字段：

- `id`
- `name`
- `goal`
- `execution_mode`
- `steps`

一句话理解：

`Complex Task` 是运营agent 的数据结构基础。

## 4.5 `planner`

中文理解：推理/分析模型角色。  
适合做：

- 分析
- 推理
- 总结
- 规划

当前推荐：

- `DeepSeek` 官方 API

## 4.6 `formatter`

中文理解：整理/结构化输出模型角色。  
适合做：

- 摘要
- 结构化 JSON 输出
- 格式化报告
- 结果包装

当前推荐：

- `火山引擎方舟` 官方 API

## 4.7 `Output Template`

中文理解：输出模板。  
作用：复用运营agent 的输出格式和结果结构。

---

## 5. 管理员手册

## 5.1 管理员主要职责

管理员通常负责：

- 配置飞连环境
- 配置双 LLM 角色
- 配置飞书企业自建应用凭证与权限
- 建立 API 模板
- 定义任务草稿
- 建立周期任务
- 编排运营agent
- 维护飞书可信设备导入
- 查看 Logs 并排障

---

## 5.2 连接设置操作指南

### 飞连连接配置

管理员进入 `连接设置` 后，优先维护飞连连接信息。

主要区域：

- `基础信息`
- `鉴权信息`
- `配置输出`

主要字段说明：

- `名称`
- `scheme`
- `host`
- `port`
- `AccessKey ID`
- `AccessKey Secret`

常用操作：

1. 点击 `加载`
2. 填写或检查飞连连接参数
3. 点击 `保存并热更新`
4. 如需调试，再点击 `测试连接`

### LLM 双模型配置

连接设置中的第二大区域是 `LLM 双模型角色配置`。

两块配置：

- `Planner 模型`
- `Formatter 模型`

管理员需要关注：

- 是否启用
- `provider`
- `base_url`
- `model`
- `api_key`
- `timeout`
- `temperature`
- `max_tokens`

Planner 额外关注：

- `thinking`
- `reasoning_effort`

Formatter 额外关注：

- `response_format.type`

常用操作：

1. 点击 `加载全部配置`
2. 配置 `planner`
3. 配置 `formatter`
4. 分别点击 `测试 Planner` / `测试 Formatter`
5. 点击 `保存并热更新`

### 飞书 API 配置（企业自建应用）

连接设置中的第三块区域是 `飞书API配置`，用于维护飞书开放平台「企业自建应用」的凭证，供「飞书可信设备导入」等模块调用飞书服务端 API 使用。

> **为什么必须是「企业自建应用」？**
> 本系统通过 `app_id` + `app_secret` 换取 `tenant_access_token`（调用接口为 `/open-apis/auth/v3/tenant_access_token/internal`），该鉴权方式仅「企业自建应用」支持。若选择「商店应用」或「第三方应用」，将无法获取本系统所需的租户级 token，导致可信设备导入失败。

#### 一、在飞书开放平台创建企业自建应用

**步骤 1：进入开发者后台**

打开飞书开放平台（https://open.feishu.cn），使用企业管理员账号登录，点击右上角「开发者后台」。

> 截图示例：`ui-screenshots/feishu/01-developer-console.png`
> （建议截取：登录后右上角下拉菜单中的「开发者后台」入口）

**步骤 2：创建企业自建应用**

1. 在开发者后台首页，点击「创建应用」。
2. 选择应用类型为 **企业自建应用**。
3. 填写应用基本信息（见下表）。
4. 点击「创建」完成应用创建。

> 截图示例：`ui-screenshots/feishu/02-create-app.png`
> （建议截取：应用类型选择页面，高亮「企业自建应用」选项）

**创建应用时需填写的必要信息：**

| 字段 | 是否必填 | 说明与建议值 |
|---|---|---|
| 应用名称 | 是 | 建议与本系统用途关联，例如「飞连可信设备同步」。名称在企业内唯一，长度 2~30 个字符。 |
| 应用描述 | 是 | 简要描述用途，例如「用于将飞连终端设备信息同步至飞书设备管理，实现可信设备联动。」 |
| 应用图标 | 是 | 上传一张不小于 144×144px 的方形图标，格式为 PNG/JPG，建议使用企业 Logo 或设备管理相关图标。 |
| 可见范围 | 否（创建时） | 创建时可暂不设置，后续在「版本管理与发布」中配置可用范围。本应用为后端服务调用，建议可见范围设置为「仅管理员可见」或指定运维部门。 |

> **注意**：应用名称创建后仍可在「凭证与基础信息」中修改；图标上传后会自动生成多尺寸缩略图。

**步骤 3：记录凭证信息**

应用创建完成后，进入应用详情页的「凭证与基础信息」，记录以下两项关键凭证：

- **App ID**：形如 `cli_xxxxxxxxxxxxxxxx`，用于标识应用。
- **App Secret**：应用密钥，仅在创建时完整展示一次，请妥善保存。如忘记可点击「重置」重新生成（重置后旧 Secret 立即失效）。

> 截图示例：`ui-screenshots/feishu/03-credentials.png`
> （建议截取：「凭证与基础信息」页面中 App ID 与 App Secret 所在区域）

#### 二、申请 API 权限

在应用详情页左侧菜单选择「权限管理」，搜索并开通以下权限。**权限开通后需发布应用版本并经企业管理员审批，审批通过后方可生效。**

本系统调用的飞书接口为「设备管理 V2」（`/open-apis/security_and_compliance/v2/device_records`），新增设备使用 POST，更新设备使用 PUT，两者均归属同一权限点。

**必填 API 权限清单：**

| 权限名称（显示名） | 权限类型 | 权限说明 |
|---|---|---|
| 增删改设备 | 接口权限 | 允许应用在飞书「设备管理」中新增、更新、删除设备记录。本系统的可信设备导入（新增 + 更新）依赖此权限。 |

**权限申请与发布流程：**

1. 进入应用 → 「权限管理」。
2. 在搜索框输入「增删改设备」或「设备」，勾选对应权限点。
3. 点击右上角「申请权限」（部分权限直接开通，高级权限需申请）。
4. 进入「版本管理与发布」→「创建版本」，填写版本说明（例如「新增可信设备导入所需的设备管理权限」）。
5. 提交发布申请，等待企业管理员在飞书管理后台审批通过。

> 截图示例：`ui-screenshots/feishu/04-permissions.png`
> （建议截取：权限管理页面搜索「增删改设备」并勾选的状态）

> **重要提示**：
> - 「增删改设备」属于高级权限，需要企业管理员审批。若审批被拒，请与管理员沟通说明用途（将飞连可信终端同步至飞书设备管理）。
> - 权限未生效前，调用接口会返回 `99991672`（无权限）或类似错误码。
> - 建议同时将应用的「数据权限范围」设置为「全部成员」或包含目标设备所属用户的部门，否则部分设备可能因数据权限不足而导入失败。

#### 三、在本系统中配置飞书 API

完成飞书侧应用创建与权限审批后，回到本系统进行配置：

1. 进入 `连接设置` 页面，点击顶部 `飞书API配置` 子页签。
2. 点击右上角 `新增`，在「飞书_API 工作台」中填写：

| 字段 | 说明 | 示例 |
|---|---|---|
| 记录 ID | 本系统内唯一标识，便于区分多套环境 | `feishu_prod_main` |
| 名称 | 配置名称 | `生产主飞书应用` |
| App ID | 飞书应用 App ID | `cli_a1b2c3d4e5f6g7h8` |
| App Secret | 飞书应用 App Secret | `xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` |
| Base URL | 飞书开放平台域名，默认即可 | `https://open.feishu.cn` |
| 启用 | 是否启用该配置 | 勾选 |

3. 勾选「保存并设为激活配置」，点击 `保存`。
4. 点击 `测试`，系统会调用 `/open-apis/auth/v3/tenant_access_token/internal` 验证凭证有效性。
   - 成功：返回 `ok: true` 且响应中包含 `tenant_access_token`。
   - 失败：检查 App ID / App Secret 是否正确，或应用是否被禁用。

> 截图示例：`ui-screenshots/feishu/05-feishu-api-config.png`
> （建议截取：连接设置 → 飞书API配置 工作台表单）

> **说明**：本系统支持保存多条飞书 API 配置，但同一时刻仅一条处于「激活」状态。切换激活配置后，`tenant_access_token` 缓存会自动失效并重新获取。

---

## 5.3 飞连API任务操作指南

### 适合什么场景

使用 `飞连API任务` 的场景包括：

- 想快速测试单个飞连接口
- 想把一类接口操作固化成任务草稿
- 想给周期任务提供执行目标

### 页面结构

主要分为：

- 飞连API列表
- 飞连API任务定义
- 输出区
- 已保存任务草稿

### 典型操作流程

1. 在飞连API列表中确认已有模板，或新增模板
2. 在 `飞连API任务定义` 中填写：
   - 草稿 ID
   - 草稿名称
   - 模式
   - 请求参数
3. 如有需要，填写：
   - 处理逻辑
   - 大模型调用
   - 输出格式
4. 点击 `Preview`
5. 点击 `Execute`
6. 结果满意后点击 `保存为任务草稿`

### 管理员注意事项

- 单次 API 请求任务应尽量保持“单目标”
- 多步骤、多模型、多次 API 调用不要继续堆在这里，应转去 `运营agent`

---

## 5.4 飞连周期任务操作指南

### 适合什么场景

使用 `飞连周期任务` 的场景包括：

- 定时拉取飞连数据
- 定时执行某条 API 任务
- 定时触发一个运营agent 工作流

### 页面结构

主要分为：

- 筛选与操作
- 调度列表
- 详情与最近运行

### 新建周期任务的典型流程

1. 点击 `新建调度`
2. 选择执行目标：
   - `task_draft`
   - `complex_task`
3. 配置调度方式：
   - `cron`
   - `interval`
4. 填写：
   - `start_at`
   - `end_at`
   - `timezone`
5. 保存并返回列表
6. 点击 `运行` 或等待调度触发

### 详情页重点关注

- `Target Type`
- `Target ID`
- `Target Name`
- `Last Run`
- `Result`
- `Duration`
- `Next Run`
- `Error`

如遇失败，优先查看：

- 最近执行失败块
- 原始响应区
- Logs 页中的执行日志

---

## 5.5 运营agent 操作指南

### 适合什么场景

使用 `运营agent` 的场景包括：

- 安全事件自动分析
- 调用多种飞连数据源进行关联判断
- 使用双模型完成“分析 + 结构化输出”
- 生成告警摘要或报告

### 页面结构

主要分为三栏：

- 左栏：运营agent 列表
- 中栏：步骤编排
- 右栏：任务配置 / 结构预览 / 运行结果 / 输出模板

### 典型操作流程

1. 点击 `新建运营agent`
2. 填写：
   - 任务 ID
   - 任务名称
   - 执行模式
   - 任务目标
3. 在中栏添加步骤：
   - `API 查询`
   - `数据处理`
   - `大模型调用`
   - `输出`
4. 在右栏配置具体步骤
5. 点击 `试运行`
6. 查看：
   - 运行摘要
   - 步骤结果
   - 最终输出
7. 保存为：
   - 运营agent
   - 任务草稿
   - 输出模板

### 文本资产

在 `llm_inference` 配置中，可使用：

- `skill_text`
- `skill_file`
- `soul_text`
- `soul_file`
- `prompt`
- `prompt_text`

### 管理员建议

- `planner` 用于分析与推理
- `formatter` 用于结果格式化和结构化输出
- 运营agent 应以多步骤链路为主，不建议退化成单 API 调用页

---

## 5.6 Logs 操作指南

### 适合什么场景

管理员使用 `Logs` 主要是为了：

- 看执行日志
- 看系统日志
- 快速定位错误

### 页面结构

- 左侧：执行日志
- 右侧：系统日志

### 重点观察项

执行日志看：

- `target_type`
- `target_id`
- `run_id`
- `SUCCESS / FAILED`
- `Last Run`
- `Duration`
- `Error`

系统日志看：

- 原始 tail 内容
- 服务异常
- 接口报错

---

## 5.7 飞书可信设备导入操作指南

### 功能概述

`飞书可信设备导入` 用于将飞连终端设备清单（含序列号、磁盘序列号、MAC 地址、可信状态等）批量导入或更新到飞书「设备管理」中，实现飞连可信终端与飞书设备管理的联动。

核心逻辑：

- **新增**：未在飞书登记过的设备，调用 `POST /open-apis/security_and_compliance/v2/device_records` 创建设备记录。
- **更新**：已在飞书登记过的设备（本系统记录了 `device_record_id`），调用 `PUT /open-apis/security_and_compliance/v2/device_records/:device_record_id` 更新设备归属与可信状态。

> 截图示例：`ui-screenshots/feishu/06-device-import-overview.png`
> （建议截取：左侧导航「飞书可信设备导入」页面整体布局）

### 前置条件

在使用本功能前，需确保以下配置已完成：

1. **飞连连接已配置并测试通过**（见 5.2「飞连连接配置」）—— 用于从飞连拉取设备清单。
2. **飞书 API 已配置并激活**（见 5.2「飞书 API 配置」）—— 用于向飞书写入设备记录。
3. **飞书应用已获得「增删改设备」权限并发布生效**。

若前置条件未满足，导入时会在输出区与 `Logs` 中报出对应错误，请先排查上述配置。

### 操作流程

#### 步骤 1：同步飞连设备清单

导入的数据源是飞连侧的设备清单，需先将其同步到本系统本地库：

1. 进入 `飞书设备清单` 页面（左侧导航「飞书 → 飞书设备清单」）。
2. 点击 `刷新`，系统调用飞连 `/api/open/v1/device/search` 分页拉取全量设备，并自动补拉每台设备的可信状态。
3. 同步完成后，列表展示设备名称、用户、部门、OS、可信状态、设备组等信息。
4. 如需定时自动同步，可开启「零点定时刷新」开关（每日 00:00 自动执行）。

> 截图示例：`ui-screenshots/feishu/07-device-list.png`
> （建议截取：飞书设备清单页面的列表与刷新按钮）

> **说明**：同步采用全量替换策略，但会保留已导入设备的 `feishu_device_record_id`，避免重复导入时丢失更新链路。

#### 步骤 2：（可选）配置字段映射

字段映射决定飞连设备的哪些字段被写入飞书设备记录。系统已内置默认映射，通常无需修改；如需调整：

1. 进入 `飞书可信设备导入` 页面，找到「字段映射」区域。
2. 默认映射关系如下：

| 飞连字段（源） | 飞书字段（目标） | 默认启用 |
|---|---|---|
| 序列号 `serial_number` | 序列号 `serial_number` | 是 |
| 硬盘序列号 `hdd_serial_numbers` | 磁盘序列号 `disk_serial_number` | 是 |
| MAC地址 `mac_addr` | MAC地址 `mac_address` | 是 |
| CPU序列号 `cpu_serial_number` | 主板UUID `uuid` | 否 |

3. 可按需启用/禁用映射行，或新增自定义映射。
4. 三个必填字段由系统自动转换，无需在映射表中配置：
   - `device_system`（操作系统）：由飞连 `os` 自动转换（Windows→1、macOS→2、Linux→3、Android→4、iOS→5）。
   - `device_ownership`（设备归属）：固定为 `2`（企业设备）。
   - `device_status`（可信状态）：由飞连 `trusted_status` 自动转换（可信→1、不可信→2、未知→0）。

> 截图示例：`ui-screenshots/feishu/08-field-mapping.png`
> （建议截取：字段映射配置区域）

#### 步骤 3：执行导入

1. 在 `飞书可信设备导入` 页面的「筛选条件」区域，可按 `OS`、`可信状态`、`设备组` 筛选需要导入的设备（留空表示全部）。
2. 选择筛选逻辑（`AND` 同时满足 / `OR` 满足任一）。
3. 点击 `预览` 查看匹配的设备数量。
4. 点击 `导入到飞书` 开始执行。
5. 导入完成后，输出区展示「总数 / 成功数」，并在 Logs 中记录每台设备的处理结果。

> 截图示例：`ui-screenshots/feishu/09-import-execute.png`
> （建议截取：导入执行后的输出结果区域）

**导入规则说明：**

- 已有 `feishu_device_record_id` 的设备 → 调用更新接口（PUT），仅更新 `device_ownership` 与 `device_status`。
- 无 `feishu_device_record_id` 的设备 → 调用新增接口（POST），成功后记录返回的 `device_record_id` 供后续更新使用。
- 若飞书返回错误码 `1785010`（设备已存在），系统会跳过该设备并记录日志，不会中断整体导入。

#### 步骤 4：（可选）开启定时导入

如需定期将飞连设备状态同步到飞书：

1. 在 `飞书可信设备导入` 页面开启「定时导入」开关。
2. 设置导入间隔（如每 6 小时）。
3. 保存后，系统会按设定周期自动执行导入（使用当前筛选条件与字段映射）。

### 常见问题与解决方案

| 问题现象 | 可能原因 | 解决方案 |
|---|---|---|
| 导入全部失败，日志提示 `app_id/app_secret is empty` | 未配置或未激活飞书 API | 在「连接设置 → 飞书API配置」中新增并激活一条配置，点击「测试」确认可获取 token |
| 导入失败，飞书返回 `code=99991672` 或提示无权限 | 应用未开通「增删改设备」权限，或权限未发布生效 | 回到飞书开放平台「权限管理」确认权限已勾选，并在「版本管理与发布」中提交发布、等待管理员审批 |
| 导入失败，飞书返回 `1785005`（设备信息与操作系统不匹配） | 字段映射的标识符与操作系统不对应 | 检查字段映射，确保 iOS 设备使用 `idfv`、Android 使用 `android_id`，桌面端使用序列号/磁盘序列号/MAC |
| 导入失败，飞书返回 `1785010`（设备已存在） | 飞书侧已存在相同标识符的设备，且无法自动关联 | 该设备会被跳过；如需更新，请先在飞书设备管理中确认设备归属，或联系飞书客服处理 |
| 部分设备导入成功，部分失败 | 个别设备缺少必填标识符 | 在飞书设备清单中检查失败设备的 `serial_number` / `disk_serial_number` / `mac_address` 是否为空；系统会优先取序列号，其次磁盘序列号，最后 MAC |
| 更新接口返回 `1785008`（设备未找到） | `device_record_id` 失效（如飞书侧已删除该设备） | 在设备清单中清除该设备的导入标记（可联系开发清理 `feishu_device_record_id`），重新走新增流程 |
| token 获取失败，提示 `get tenant_access_token failed` | App Secret 错误或应用被禁用 | 核对 App ID / App Secret；在飞书开放平台确认应用状态为「已启用」 |

### 验证导入结果

导入完成后，可通过以下方式验证：

1. **本系统侧**：在「飞书设备清单」中确认设备已带有 `feishu_device_record_id`（表示已成功导入飞书）。
2. **飞书侧**：登录飞书管理后台 →「安全与合规」→「设备管理」，查看对应设备是否已登记，以及可信状态是否与飞连一致。
3. **日志侧**：在 `Logs` 中过滤 `[飞书导入]` 关键字，查看每台设备的新增/更新结果与失败原因。

---

## 5.8 自动化审批操作指南

### 功能概述

`自动化审批` 用于监听飞书「设备申报」事件，对管理员指定白名单分组内的设备**自动通过审批**（调用飞书新增设备接口，登记为公司设备 + 信任设备），其余设备留存为**待审批任务**并通过 Webhook 推送通知，支持人工通过或驳回。

核心逻辑：

- **事件接入**：接收飞书事件 `security_and_compliance.device_apply_record.device_apply_event_v2`，端点为 `POST /api/v1/approval/webhook`。
- **设备匹配**：从事件体 `event.device_record` 中提取设备标识（优先级：序列号 → MAC → UUID → 磁盘序列号 → 设备名），在飞连设备清单中查询。
- **白名单判定**：若设备所属分组命中管理员配置的白名单分组，则自动通过；否则留存待审批。
- **自动通过**：调用飞书 `POST /open-apis/security_and_compliance/v2/device_records`，`device_ownership=2`（公司设备）、`device_status=1`（信任设备）。
- **待审批通知**：通过「连接设置 → Webhook配置」中选定的 Webhook，将完整事件体推送出去。
- **人工处理**：管理员可在任务列表中对 `pending` 状态的任务手动「通过」或「驳回」。

### 前置条件

1. **飞连连接已配置并测试通过**（见 5.2）—— 用于在飞连中查询设备。
2. **飞书 API 已配置并激活**（见 5.2）—— 用于向飞书写入设备记录。
3. **飞书应用已获得「增删改设备」权限并发布生效**。
4. **设备分组已同步**（见「设备分组」页面）—— 白名单分组来源于飞连设备分组。
5. **飞书事件订阅已配置**：在飞书开放平台「事件订阅」中添加请求地址 `http(s)://<本服务地址>/api/v1/approval/webhook`，并订阅事件 `security_and_compliance.device_apply_record.device_apply_event_v2`。

> 飞书要求事件回调地址需公网可访问，且 3 秒内返回 200；本系统收到事件后立即响应，审批逻辑在后台异步执行。

### 操作流程

#### 步骤 1：配置白名单分组与 Webhook

1. 进入 `自动化审批` 页面（左侧导航「小插件 → 自动化审批」）。
2. 在「审批配置」区域：
   - 勾选「启用自动化审批」。
   - 在「白名单设备分组」中勾选允许自动通过的分组（多选，命中任一即自动通过）。
   - 在「待审批推送 Webhook」下拉中选择一个已配置的 Webhook（用于接收未命中白名单的设备通知）；不选则不推送。
3. 点击「保存配置」。

> 若分组列表为空，请先到「设备分组」页面点击「同步」拉取飞连分组。

#### 步骤 2：在飞书开放平台配置事件订阅

1. 进入飞书开放平台 → 对应企业自建应用 →「事件订阅」。
2. 请求地址填写本系统页面底部展示的「Webhook 回调地址」（格式：`http(s)://<host>/api/v1/approval/webhook`）。
3. 飞书会发送 `url_verification` 校验请求，本系统自动返回 `challenge`，配置保存即可通过校验。
4. 添加事件 `security_and_compliance.device_apply_record.device_apply_event_v2`。
5. 发布应用版本并经管理员审批生效。

#### 步骤 3：查看与处理审批任务

1. 在「审批任务」区域可查看所有事件流水，支持按状态筛选（待审批 / 自动通过 / 人工通过 / 已驳回）。
2. 对 `pending` 状态的任务：
   - 点击「通过」→ 系统在飞连中查询该设备并调用飞书新增设备接口，状态变为 `approved`。
   - 点击「驳回」→ 状态变为 `rejected`。
3. 任务列表展示任务 ID、设备标识、状态、飞书设备记录 ID、错误信息、创建时间。

### 状态说明

| 状态 | 含义 |
|---|---|
| `pending` | 待审批（未命中白名单 / 飞连未找到设备 / 处理异常） |
| `auto_approved` | 自动通过（命中白名单，已自动调用飞书新增设备接口） |
| `approved` | 人工通过 |
| `rejected` | 人工驳回 |

### 常见问题与解决方案

| 问题现象 | 可能原因 | 解决方案 |
|---|---|---|
| 飞书事件订阅校验失败 | 回调地址不可达或返回非 200 | 确认本服务公网可访问；检查 `/api/v1/approval/webhook` 是否返回 `{"challenge":"xxx"}` |
| 设备全部留存为 pending，无自动通过 | 白名单分组未配置，或设备不在白名单分组内 | 检查白名单分组配置；确认飞连设备的 `groups_id` 与所选分组一致 |
| 自动通过失败，任务 error_message 提示飞书无权限 | 飞书应用未开通「增删改设备」权限 | 见 5.7 常见问题，回到飞书开放平台开通权限并发布 |
| 飞连未找到该设备 | 事件中的设备标识（序列号/MAC）在飞连设备清单中不存在 | 先在「设备分组 / 飞书设备清单」同步飞连设备；确认设备标识字段非空 |
| 待审批任务未收到 Webhook 通知 | 未配置 Webhook 或 Webhook 配置 ID 失效 | 在「审批配置」中重新选择有效的 Webhook；可在「连接设置 → Webhook配置」测试连通性 |
| 同一事件重复创建任务 | 事件 `event_id` 为空 | 飞书事件均带 `event_id`，系统按此幂等去重；若为空则无法去重 |

### 验证结果

1. **本系统侧**：在「自动化审批」任务列表中，命中白名单的事件状态为 `auto_approved` 并带有 `feishu_device_record_id`。
2. **飞书侧**：登录飞书管理后台 →「安全与合规」→「设备管理」，确认设备已登记为公司设备 + 信任设备。
3. **日志侧**：在 `Logs` 中过滤 `[自动化审批]` 关键字，查看事件处理过程与失败原因。

---

## 6. 普通使用者手册

## 6.1 普通使用者通常做什么

普通使用者通常不会创建底层连接和任务结构，而是：

- 打开 Dashboard 查看整体状态
- 打开飞连周期任务查看现有调度结果
- 打开运营agent 查看运行结果
- 在 Logs 中查看最近执行情况

---

## 6.2 Dashboard 怎么看

进入首页后，优先关注：

- `服务健康`
- 核心模块数量
- 双模型角色数量
- 运行模式

普通使用者一般不需要改配置，只需要通过这里快速进入目标页面。

---

## 6.3 飞连周期任务怎么用

普通使用者使用 `飞连周期任务` 时，重点不是创建，而是：

- 查看任务列表
- 筛选任务
- 查看最近状态
- 查看最近错误
- 必要时手动运行一次

建议重点看：

- 任务是否启用
- 最近一次是否成功
- 最近错误是什么

---

## 6.4 运营agent 怎么看结果

普通使用者进入 `运营agent` 后，重点看右侧区域：

- 任务目标
- 结构预览
- 运行摘要
- 步骤结果
- 最终输出

如果只看结论：

- 优先看 `运行摘要`
- 再看 `最终输出`

如果要追根因：

- 继续看每一步的状态条
- 看红色错误块

---

## 6.5 Logs 怎么辅助看问题

当普通使用者发现：

- 周期任务没跑成功
- 运营agent 没出结果
- 页面结果异常

可以进入 `Logs`：

- 先看左边执行日志
- 再看右边系统日志

如果看到：

- 绿色成功块：通常表示链路本身成功
- 红色错误块：优先复制其中的错误信息给管理员或研发

---

## 7. 常见问题

## 7.1 为什么现在不再叫 API 工具箱、Jobs、复杂任务？

因为新版结构更强调产品职责：

- `飞连API任务` 表示单次任务定义
- `飞连周期任务` 表示调度中心
- `运营agent` 表示智能体工作流

新名称比旧名称更贴合实际用途。

## 7.2 什么时候用飞连API任务，什么时候用运营agent？

简单判断：

- 单次接口任务：用 `飞连API任务`
- 多步骤分析和智能处理：用 `运营agent`

## 7.3 什么时候查看 Logs？

当出现以下情况时：

- 运行失败
- 没有输出
- 结果异常
- 不确定是配置问题还是执行问题

就应该先去 `Logs`。

## 7.4 Planner 和 Formatter 有什么区别？

- `planner`：更适合分析、规划、推理
- `formatter`：更适合整理、摘要、结构化输出

---

## 8. 文件与数据说明

当前系统中最常见的数据文件有：

- `config.yaml`
- `connections.yaml`
- `api-templates.yaml`
- `task-drafts.yaml`
- `job-schedules.yaml`
- `complex-tasks.yaml`
- `output-templates.yaml`
- `job-runs.json`

它们分别负责：

- 基础配置
- 连接历史
- API 模板
- 任务草稿
- 周期任务
- 运营agent 工作流
- 输出模板
- 最近运行状态

---

## 9. 总结

新版飞连运维控制台的核心逻辑可以用一句话概括：

- `连接设置`：配环境和模型
- `飞连API任务`：定义单次任务
- `飞连周期任务`：做定时调度
- `运营agent`：做自动分析与告警
- `Logs`：做排障与追踪

这就是管理员和普通使用者在新版界面中的主要使用路径。

---

## 10. 变更记录

| 日期 | 版本 | 更新内容 | 关联 |
|---|---|---|---|
| 2026-09-21 | v1.3 | 新增「自动化审批」功能：监听飞书设备申报事件，白名单分组设备自动通过（公司设备+信任设备），其余留存待审批并推送 Webhook，支持人工通过/驳回。新增 2.8、5.8 章节。 | 小插件 · 自动化审批 |
