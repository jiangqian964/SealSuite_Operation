# 飞连运维控制台使用手册

版本基线：当前代码库实现状态  
适用对象：管理员、普通使用者、二次维护人员  
文档目标：说明当前系统怎么用，并详细解释系统中每一个核心名称的含义

---

## 1. 系统是什么

飞连运维控制台是一个本地 Web 控制台，用来统一完成以下几类工作：

- 配置飞连连接参数
- 获取并验证飞连 `access_token`
- 调试和执行 API 请求
- 把请求定义保存成“任务草稿”
- 把任务草稿绑定成周期调度
- 编排“复杂任务”工作流
- 在复杂任务里串联 API、数据处理、大模型调用和输出
- 查看运行结果、日志、调试信息

你可以把它理解成三层结构：

- `连接设置`：解决“连到哪里、凭什么连”
- `API 工具箱`：解决“要做什么、怎么做”
- `Jobs`：解决“什么时候自动做”
- `复杂任务`：解决“当事情不再是单步接口调用，而是多步链路时怎么做”

---

## 2. 系统首页导航含义

左侧导航栏中每个名字的含义如下。

### 2.1 `Dashboard`

含义：总览页。  
作用：显示服务健康状态和常用入口。  
适合谁：所有人。

你通常会在这里做三件事：

- 确认服务是否正常
- 快速进入连接设置
- 快速进入 API 工具箱或 Jobs

页面中的名称说明：

- `健康状态`：当前 Web 服务是否可响应
- `Health`：手动刷新健康检查
- `Reload`：重新加载运行时配置和任务定义
- `Healthy / Unknown / Error`：健康状态显示值

### 2.2 `连接设置`

含义：配置飞连连接和大模型连接的页面。  
作用：保存连接参数、测试连接、热更新配置。  
适合谁：管理员。

### 2.3 `Jobs`

含义：调度中心。  
作用：把已经定义好的任务草稿绑定成定时执行计划。  
适合谁：管理员。

### 2.4 `API 工具箱`

含义：任务定义工作台。  
作用：定义单步 API 请求、处理逻辑、LLM 调用和输出格式，并保存为任务草稿。  
适合谁：管理员、高级使用者。

### 2.5 `复杂任务`

含义：多步骤工作流编排页面。  
作用：把多个步骤组合起来，形成一个可试运行、可保存、可复用的复杂流程。  
适合谁：管理员、高级使用者。

### 2.6 `Logs`

含义：日志查看页。  
作用：查看服务日志尾部内容。  
适合谁：管理员、排障人员。

---

## 3. 系统中的核心概念与名称解释

这一节是整份手册最重要的“名词解释”部分。

### 3.1 `Template`

中文理解：API 模板。  
含义：一个预定义好的 API 请求结构。  
作用：把 `method`、`path`、参数结构、dry-run 支持等固定下来，方便复用。

系统中的模板主要保存在 `api-templates.yaml`。

模板常见字段含义：

- `id`：模板唯一标识
- `name`：模板显示名称
- `category`：模板分类，例如 `users`、`devices`、`policy`
- `method`：HTTP 方法，例如 `GET`、`POST`、`PUT`
- `path`：API 路径，例如 `/api/v1/users`
- `query_schema`：查询参数结构定义
- `path_params_schema`：路径参数结构定义
- `body_schema`：请求体结构定义
- `dry_run_query_param`：若接口支持 dry-run，这里定义实际参数名

一句话理解：

`Template` 是“请求模板”，不是任务，不带时间计划。

### 3.2 `Task Draft`

中文理解：任务草稿。  
含义：一个已经定义好执行逻辑、但还没绑定调度时间的任务定义。  
作用：连接 `API 工具箱` 与 `Jobs`。

系统中的任务草稿保存在 `task-drafts.yaml`。

任务草稿字段含义：

- `id`：草稿唯一标识
- `name`：草稿名称
- `mode`：草稿模式
- `source_template_id`：来源模板 ID
- `input_config`：输入配置
- `transform_config`：处理配置
- `llm_config`：大模型配置
- `output_config`：输出配置

`mode` 的含义：

- `api_only`：只有 API 调用
- `api_plus_process`：API 调用 + 数据处理
- `api_plus_llm`：API 调用 + 大模型
- `workflow`：完整工作流模式

一句话理解：

`Task Draft` 是“任务内容”，但还不是“定时任务”。

### 3.3 `Job Schedule`

中文理解：调度计划。  
含义：一个绑定了 `Task Draft` 的执行时间计划。  
作用：决定任务什么时候执行。

系统中的调度计划保存在 `job-schedules.yaml`。

字段含义：

- `id`：调度 ID
- `draft_id`：绑定的任务草稿 ID
- `enabled`：是否启用
- `start_at`：开始时间
- `end_at`：结束时间
- `schedule_type`：调度类型
- `cron`：cron 表达式
- `interval`：固定间隔，例如 `30m`
- `timezone`：时区

`schedule_type` 的含义：

- `cron`：使用 cron 表达式
- `interval`：按固定时间间隔循环执行

一句话理解：

`Job Schedule` 只负责“什么时候执行哪个草稿”。

### 3.4 `Complex Task`

中文理解：复杂任务。  
含义：一个多步骤工作流。  
作用：把 API 查询、处理、大模型、输出串起来。

系统中的复杂任务保存在 `complex-tasks.yaml`。

字段含义：

- `id`：复杂任务唯一标识
- `name`：复杂任务名称
- `goal`：任务目标说明
- `execution_mode`：执行模式
- `steps`：步骤列表

`execution_mode` 的含义：

- `workflow`：普通工作流模式
- `agent_assisted`：预留给 Agent 增强模式
- `manual_checkpoint`：预留给人工确认节点模式

一句话理解：

`Complex Task` 是“多步骤任务流程”，不是单接口请求。

### 3.5 `Complex Task Step`

中文理解：复杂任务步骤。  
含义：复杂任务中的单个执行节点。  
作用：作为流程里的一个环节。

字段含义：

- `id`：步骤 ID
- `type`：步骤类型
- `name`：步骤名称
- `config`：步骤配置

当前支持的步骤类型：

- `api_call`：调用接口
- `data_transform`：处理数据
- `llm_inference`：调用大模型
- `output`：生成输出

### 3.6 `Output Template`

中文理解：输出模板。  
含义：可复用的结果输出格式定义。  
作用：让复杂任务的输出步骤可以复用已有的结果格式。

系统中的输出模板保存在 `output-templates.yaml`。

字段含义：

- `id`：输出模板 ID
- `name`：输出模板名称
- `format`：输出格式
- `title`：输出标题
- `content`：输出内容
- `source_complex_task_id`：来源复杂任务 ID
- `output_config`：输出配置
- `meta`：附加元信息

一句话理解：

`Output Template` 不是接口模板，而是“结果格式模板”。

### 3.7 `LLM`

中文理解：大语言模型。  
作用：在复杂任务中对前一步结果做总结、改写、解释、归纳。

当前系统采用的是 OpenAI 兼容接口方式。

LLM 配置字段含义：

- `enabled`：是否启用 LLM
- `provider`：提供方类型，当前建议 `openai_compatible`
- `base_url`：大模型接口根地址
- `api_key`：调用密钥
- `model`：模型名称
- `timeout`：超时时间
- `mock_mode`：是否模拟模式
- `system_prompt`：系统提示词
- `temperature`：采样温度
- `max_tokens`：最大输出 token 数

### 3.8 `access_token`

中文理解：飞连访问令牌。  
作用：真正访问飞连业务接口时的授权凭证。

系统行为是：

- 先用 `AccessKey ID + AccessKey Secret` 请求 `access_token`
- 成功后自动缓存
- 后续业务请求自动带上 `Authorization`

你不需要手工填 `Authorization` 头。

---

## 4. 页面使用说明

## 4.1 Dashboard 使用说明

适合做什么：

- 确认系统是否正常
- 进入主要功能页
- 快速执行刷新和重载

推荐操作：

1. 打开页面先看 `健康状态`
2. 如果不是 `Healthy`，先检查日志
3. 如刚改过配置，点 `Reload`

---

## 4.2 连接设置使用说明

本页由两部分组成：

- 飞连连接配置
- LLM 配置

### 4.2.1 飞连连接配置字段说明

- `名称（可选）`：仅用于历史列表显示，例如“生产环境”“测试环境”
- `scheme`：协议，通常是 `https`
- `host`：飞连域名或 IP
- `port`：端口，例如 `443`
- `AccessKey ID`：飞连访问密钥 ID
- `AccessKey Secret`：飞连访问密钥 Secret

按钮含义：

- `加载`：读取当前保存到 `config.yaml` 中的配置
- `测试连接`：临时测试当前输入是否能拿到 token，并验证接口可访问
- `保存并热更新`：保存到 `config.yaml`，并立即让服务生效
- `刷新`：刷新最近启用的连接记录
- `切换`：把某条历史连接重新设为当前启用配置

### 4.2.2 最近启用记录含义

这是最近使用过并保存的飞连连接记录，最多保留 3 条。  
它只保存以下信息：

- 地址
- AccessKey ID（脱敏）
- 是否当前启用

它不会保存或回显 Secret 明文。

### 4.2.3 测试连接结果怎么看

测试连接弹窗里有两块：

- `Token 获取`
- `API 探测`

字段含义：

- `token_ok`：是否拿到有效 token
- `token_preview`：脱敏后的 token 片段
- `token_expires_in`：token 有效期
- `token_error`：token 获取错误
- `probe_ok`：接口探测是否成功
- `probe_result`：探测接口返回结果
- `probe_error`：探测错误信息

### 4.2.4 LLM 配置使用说明

字段含义：

- `启用`：是否启用 LLM 调用
- `provider`：接口类型，当前建议填 `openai_compatible`
- `base_url`：例如 `https://api.openai.com/v1`
- `model`：例如 `gpt-4.1-mini`
- `api_key`：密钥
- `timeout(s)`：超时秒数
- `temperature`：生成温度
- `max_tokens`：最大输出长度
- `system_prompt`：全局系统提示词

按钮含义：

- `加载`：读取当前 LLM 配置
- `测试 LLM`：测试当前配置是否能成功请求模型
- `保存并热更新`：写入 `config.yaml` 并立即生效

---

## 4.3 Jobs 使用说明

本页只管理调度，不定义复杂参数。

### 4.3.1 列表字段说明

- `schedule_id`：调度计划 ID
- `draft_id`：该调度绑定的任务草稿 ID
- `type`：调度类型，即 `cron` 或 `interval`
- `plan`：执行计划内容
- `enabled`：启用状态
- `actions`：操作按钮

按钮含义：

- `刷新`：刷新调度列表
- `新建调度`：创建新的调度计划
- `Reload`：让后端重新加载配置和调度
- `运行`：手动触发执行一次
- `编辑`：打开快速编辑
- `启用 / 停用`：切换调度状态
- `删除`：删除该调度

### 4.3.2 调度详情含义

详情区会显示：

- 调度基础信息
- 绑定的任务草稿
- 最近一次运行结果
- 对应草稿摘要

运行结果字段含义：

- `Last Run`：最近执行时间
- `Result`：成功或失败
- `Duration`：执行耗时
- `Next Run`：下一次计划执行时间
- `Error`：错误信息

### 4.3.3 新建调度的前提

必须先在 `API 工具箱` 里保存至少一个任务草稿。  
没有草稿时，不能新建调度。

---

## 4.4 API 工具箱使用说明

本页是“任务定义工作台”，不是简单的接口调试器。

### 4.4.1 飞连API列表区域说明

字段含义：

- `id`：模板 ID
- `category`：模板分类
- `method`：HTTP 方法
- `path`：接口路径

按钮含义：

- `刷新`：重新拉取飞连API列表
- `新增模板`：新建 API 模板
- `测试`：测试该模板对应接口
- `编辑`：修改模板
- `删除`：删除模板

### 4.4.2 任务定义工作台字段说明

#### 草稿信息

- `草稿 ID`：任务草稿唯一标识
- `草稿名称`：草稿显示名称
- `模式`：草稿模式，决定这是 API-only 还是带处理/LLM/工作流

#### 输入参数

- `template_id`：引用已有 API 模板
- `method`：手工指定请求方法
- `path`：手工指定请求路径
- `query(JSON)`：查询参数
- `path_params(JSON)`：路径参数
- `body(JSON)`：请求体

说明：

- 你可以只用 `template_id`
- 也可以直接手工填 `method + path`
- 两者都为空时，任务定义不完整

#### 处理逻辑

- `draft-transform` 对应 `transform_config`
- 用于定义数据变换规则

#### 大模型调用

- `draft-llm` 对应 `llm_config`
- 用于定义 LLM 提示词、模型覆盖配置等

#### 输出格式

- `draft-output` 对应 `output_config`
- 用于定义输出格式，例如 `markdown`、`json`、`text`

### 4.4.3 按钮含义

- `Preview`：预览执行效果，适合写入前确认
- `Execute`：执行当前定义
- `保存为任务草稿`：将当前工作台内容保存为 `Task Draft`
- `Copy`：复制输出结果

### 4.4.4 已保存任务草稿区域

每条草稿支持：

- `加载`：把草稿内容回填到工作台
- `删除`：删除草稿

草稿用途：

- 给 Jobs 绑定调度
- 作为复杂任务的基础定义参考

---

## 4.5 复杂任务使用说明

这是当前系统最强的页面。

### 4.5.1 左侧：任务列表

作用：查看、加载、删除复杂任务。

按钮含义：

- `新建复杂任务`：创建空白复杂任务
- `加载`：把已有任务加载到中间和右侧编辑区
- `删除`：删除任务

### 4.5.2 中间：步骤编排

作用：配置流程步骤顺序。

按钮含义：

- `API 查询`：添加 `api_call` 步骤
- `数据处理`：添加 `data_transform` 步骤
- `大模型调用`：添加 `llm_inference` 步骤
- `输出`：添加 `output` 步骤

每个步骤上还有：

- `上移`：向前移动
- `下移`：向后移动
- `复制`：复制一个步骤
- `查看`：选中并编辑该步骤
- `删除`：删除该步骤

另外还支持：

- 拖拽排序

### 4.5.3 右侧：任务配置 / 步骤配置

任务级字段：

- `任务 ID`：复杂任务唯一 ID
- `任务名称`：显示名称
- `执行模式`：当前支持 `workflow`，其余为预留扩展
- `任务目标`：描述任务要做什么

选中某个步骤后，会显示步骤配置编辑器。

步骤编辑器中的名称说明：

- `步骤名称`：步骤显示名
- `步骤类型`：只读，表示当前步骤属于哪一类
- `步骤配置(JSON)`：步骤执行所需配置
- `从当前工作台回填`：仅 `API 查询` 步骤可用，把 API 工具箱里的输入回填到该步骤
- `格式化 JSON`：整理配置 JSON

### 4.5.4 复杂任务中的步骤类型解释

#### `api_call`

含义：调用接口。  
常用配置：

- `template_id`
- `method`
- `path`
- `query`
- `path_params`
- `body`

#### `data_transform`

含义：对前一步结果做数据处理。  
当前基础支持：

- `passthrough`
- `json_map`
- `wrap`

如果写了当前系统未实现的类型，系统会返回原始输入并提示未实现。

#### `llm_inference`

含义：把前一步结果送给大模型处理。  
常用配置：

- `provider`
- `base_url`
- `api_key`
- `model`
- `prompt`
- `system_prompt`
- `temperature`
- `max_tokens`
- `enabled`
- `mock_mode`

执行逻辑：

- 若全局 LLM 配置完整，会真正调用模型
- 若步骤内有局部配置，会覆盖全局配置
- 若配置不完整，会降级成预览结果，而不是直接崩掉

#### `output`

含义：把前一步结果整理成最终输出。  
当前支持：

- `format = json`
- `format = text`
- `format = markdown`
- `content_template`

`content_template` 的含义：

- 自定义输出模板字符串
- 支持 `{{result}}` 或 `{{json}}` 占位

### 4.5.5 试运行与结果区

按钮含义：

- `试运行`：运行当前编辑态复杂任务，不必先保存
- `保存为任务草稿`：把复杂任务抽取成 `Task Draft`
- `保存为输出模板`：把当前试运行结果保存成 `Output Template`
- `保存复杂任务`：保存任务定义本身
- `删除复杂任务`：删除当前任务

运行结果区包含：

- `运行摘要`
- `步骤结果`
- `最终输出`

字段含义：

- `SUCCESS / FAILED`：整体是否成功
- `Started`：开始时间
- `Finished`：结束时间
- `Steps`：执行步骤数量
- `Error`：整体错误

每个步骤结果卡片中的字段：

- 步骤序号
- 步骤名称
- `step_id`
- 步骤类型
- `OK / ERROR`
- 步骤输出
- 步骤错误

### 4.5.6 已保存输出模板区域

作用：复用已保存的输出格式。

按钮含义：

- `刷新`：刷新输出模板列表
- `应用到输出步骤`：把该模板回填到当前复杂任务的 `output` 步骤
- `删除`：删除该输出模板

---

## 4.6 Logs 使用说明

字段含义：

- `tail`：查看日志尾部的字符数或行数范围参数
- `刷新`：重新读取日志

用途：

- 看运行错误
- 看接口报错
- 看调度异常

---

## 5. 推荐使用流程

## 5.1 场景一：只想调一个接口

1. 进入 `连接设置`
2. 配好飞连连接并测试
3. 进入 `API 工具箱`
4. 选择模板或手工输入接口
5. 填参数
6. 点 `Preview` 或 `Execute`

## 5.2 场景二：想做周期任务

1. 在 `API 工具箱` 定义好任务
2. 点 `保存为任务草稿`
3. 进入 `Jobs`
4. 点 `新建调度`
5. 绑定 `draft_id`
6. 配 `cron` 或 `interval`
7. 启用调度

## 5.3 场景三：想做复杂工作流

1. 进入 `复杂任务`
2. 新建任务
3. 添加 `api_call`
4. 添加 `data_transform` 或 `llm_inference`
5. 添加 `output`
6. 点 `试运行`
7. 结果满意后保存
8. 如需复用，可保存为任务草稿或输出模板

## 5.4 场景四：想让复杂任务做智能总结

1. 在 `连接设置` 中配置 `LLM`
2. 在 `复杂任务` 中添加 `llm_inference`
3. 配 `prompt`
4. 试运行
5. 查看大模型输出

---

## 6. 配置文件与数据文件说明

### 6.1 `config.yaml`

系统主配置文件。  
当前主要包含：

- `sealsuite`
- `llm`
- `scheduler`
- `server`
- `log`

### 6.2 `connections.yaml`

最近启用的飞连连接历史记录。

### 6.3 `api-templates.yaml`

API 模板库。

### 6.4 `task-drafts.yaml`

任务草稿库。

### 6.5 `job-schedules.yaml`

新调度中心使用的调度计划库。

### 6.6 `complex-tasks.yaml`

复杂任务工作流定义库。

### 6.7 `output-templates.yaml`

输出模板库。

### 6.8 `job-runs.json`

最近运行记录存储文件。

### 6.9 `jobs.yaml`

旧版 Jobs 模型存储。  
说明：当前系统仍保留旧接口兼容，但新页面主流程已经切到 `task-drafts + job-schedules`。

---

## 7. API 接口名称说明

这里列出当前系统中最重要的后端接口，并说明它们是做什么的。

### 7.1 健康与基础

- `GET /api/v1/health`：健康检查
- `GET /api/v1/version`：读取版本
- `POST /api/v1/reload`：重新加载配置和任务

### 7.2 任务草稿

- `GET /api/v1/task-drafts`：列出草稿
- `GET /api/v1/task-drafts/{id}`：查看草稿详情
- `POST /api/v1/task-drafts`：创建草稿
- `PUT /api/v1/task-drafts/{id}`：更新草稿
- `DELETE /api/v1/task-drafts/{id}`：删除草稿

### 7.3 调度计划

- `GET /api/v1/job-schedules`：列出调度
- `GET /api/v1/job-schedules/{id}`：查看调度详情
- `POST /api/v1/job-schedules`：创建调度
- `PUT /api/v1/job-schedules/{id}`：更新调度
- `DELETE /api/v1/job-schedules/{id}`：删除调度
- `POST /api/v1/job-schedules/{id}/run`：手动执行一次

### 7.4 复杂任务

- `GET /api/v1/complex-tasks`：列出复杂任务
- `GET /api/v1/complex-tasks/{id}`：查看复杂任务
- `POST /api/v1/complex-tasks`：创建复杂任务
- `PUT /api/v1/complex-tasks/{id}`：更新复杂任务
- `DELETE /api/v1/complex-tasks/{id}`：删除复杂任务
- `POST /api/v1/complex-tasks/run`：运行当前编辑态复杂任务
- `POST /api/v1/complex-tasks/{id}/run`：运行已保存复杂任务

### 7.5 输出模板

- `GET /api/v1/output-templates`：列出输出模板
- `GET /api/v1/output-templates/{id}`：查看输出模板
- `POST /api/v1/output-templates`：创建或保存输出模板
- `DELETE /api/v1/output-templates/{id}`：删除输出模板

### 7.6 飞连连接

- `GET /api/v1/connection`：读取当前飞连连接
- `PUT /api/v1/connection`：更新飞连连接
- `POST /api/v1/connection/test`：测试飞连连接
- `GET /api/v1/connections`：读取历史连接
- `POST /api/v1/connections`：保存当前连接并写历史
- `POST /api/v1/connections/{id}/activate`：切换某条历史连接为当前连接

### 7.7 LLM 配置

- `GET /api/v1/llm/config`：读取当前 LLM 配置
- `PUT /api/v1/llm/config`：更新 LLM 配置
- `POST /api/v1/llm/test`：测试 LLM 连通性

### 7.8 API 工具箱

- `GET /api/v1/api/templates`：列出 API 模板
- `GET /api/v1/api/templates/{id}`：查看模板
- `POST /api/v1/api/templates`：创建模板
- `PUT /api/v1/api/templates/{id}`：更新模板
- `DELETE /api/v1/api/templates/{id}`：删除模板
- `POST /api/v1/api/templates/{id}/test`：测试模板
- `POST /api/v1/api/execute`：执行 API
- `POST /api/v1/api/preview`：预览 API 执行
- `GET /api/v1/api/history`：读取历史
- `POST /api/v1/api/save-as-job`：旧模型下保存为任务

### 7.9 旧版 Jobs 兼容接口

当前仍保留：

- `GET /api/v1/jobs`
- `GET /api/v1/jobs/{name}`
- `POST /api/v1/jobs`
- `PUT /api/v1/jobs`
- `PUT /api/v1/jobs/{name}`
- `DELETE /api/v1/jobs/{name}`
- `POST /api/v1/jobs/{name}/enable`
- `POST /api/v1/jobs/{name}/disable`
- `POST /api/v1/jobs/{name}/run`

说明：这些接口主要为旧逻辑兼容保留，新 UI 主流程优先使用 `job-schedules`。

### 7.10 日志

- `GET /api/v1/logs`：读取日志

---

## 8. 字段名对照表

这一节按“页面显示名 -> 存储字段名”的方式做对照。

### 8.1 飞连连接字段对照

- `scheme` -> `scheme`
- `host` -> `host`
- `port` -> `port`
- `AccessKey ID` -> `access_key`
- `AccessKey Secret` -> `secret_key`

### 8.2 LLM 字段对照

- `启用` -> `enabled`
- `provider` -> `provider`
- `base_url` -> `base_url`
- `api_key` -> `api_key`
- `model` -> `model`
- `timeout(s)` -> `timeout`
- `temperature` -> `temperature`
- `max_tokens` -> `max_tokens`
- `system_prompt` -> `system_prompt`

### 8.3 调度字段对照

- `schedule_id` -> `id`
- `draft_id` -> `draft_id`
- `enabled` -> `enabled`
- `type` -> `schedule_type`
- `plan` -> `cron` 或 `interval`

### 8.4 复杂任务字段对照

- `任务 ID` -> `id`
- `任务名称` -> `name`
- `任务目标` -> `goal`
- `执行模式` -> `execution_mode`
- `步骤` -> `steps`

### 8.5 复杂任务步骤字段对照

- `步骤 ID` -> `id`
- `步骤类型` -> `type`
- `步骤名称` -> `name`
- `步骤配置` -> `config`

---

## 9. 当前系统边界与注意事项

### 9.1 真实已支持

- 飞连连接保存与热更新
- token 获取和业务探测
- API 模板管理
- 任务草稿管理
- 调度中心
- 复杂任务编排
- 基础 LLM 接入
- 输出模板保存与复用

### 9.2 当前仍有限制

- 当前环境下无法在这里直接为你运行完整 Go 回归测试
- `data_transform` 还是基础版
- `agent_assisted` 和 `manual_checkpoint` 目前更多是模式预留
- 旧版 `jobs.yaml` 仍保留兼容逻辑，新旧模型并存

### 9.3 对管理员的建议

- 先维护好飞连和 LLM 两套连接配置
- 新任务优先走 `Task Draft + Job Schedule`
- 复杂链路优先走 `Complex Task`
- 输出沉淀优先保存成 `Output Template`

---

## 10. 常见问题

### 10.1 为什么我在 Jobs 里看不到 Path 和 Method？

因为新设计里：

- `API 工具箱` 负责定义任务内容
- `Jobs` 只负责时间计划

所以 `Path / Method` 应该在 `API 工具箱` 里定义，而不是在 `Jobs` 里重复填写。

### 10.2 `Authorization` 和 `Content-Type` 需要自己填吗？

通常不需要。

系统会：

- 自动获取 `access_token`
- 自动注入 `Authorization`
- 自动带 `Content-Type`

### 10.3 什么时候用任务草稿，什么时候用复杂任务？

简单规则：

- 单步接口或轻量流程：用 `任务草稿`
- 多步骤、需要处理、需要总结：用 `复杂任务`

### 10.4 什么时候保存为输出模板？

当你希望“以后多个复杂任务复用同一种输出格式”时，就保存为输出模板。

### 10.5 LLM 配了但仍然没真正调用怎么办？

检查以下几点：

- `enabled` 是否为 `true`
- `base_url` 是否正确
- `api_key` 是否正确
- `model` 是否填写
- 目标服务是否真的是 OpenAI 兼容接口

---

## 11. 一句话总结构图

如果只记一句话，请记住：

- `连接设置`：配连接
- `API 工具箱`：定义任务
- `Jobs`：定时间
- `复杂任务`：做工作流
- `输出模板`：复用结果格式

这就是当前系统的核心使用逻辑。
